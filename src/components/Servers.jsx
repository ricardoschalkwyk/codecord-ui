import React from "react";
import PropTypes from "prop-types";

const Servers = () => {
  return (
    <div className="hidden h-screen w-[72px] flex-shrink-0 bg-discord-dark lg:block"></div>
  );
};

Servers.propTypes = {};

export default Servers;
