import React from "react";
import PropTypes from "prop-types";

const TopBar = () => {
  return (
    <div className="absolute z-10 h-12 w-full bg-discord-primary shadow"></div>
  );
};

TopBar.propTypes = {};

export default TopBar;
